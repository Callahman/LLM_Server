# pymumble pulls in opuslib (needs libopus-dev to build) and protobuf
# NOTE: pymumble 1.7 is installed from a VENDED WHEEL (../vendor), not a URL.
# The original source (steff85/pymumble @ 1.7) now 404s on GitHub, the PyPI
# release and the azlux fork still use ssl.wrap_socket (removed in Python
# 3.12), and 1.7 is the version with the modern SSLContext API + client_type.
# Each machine's 1.7 wheel was built locally during the original install and
# is committed under vendor/ — pip selects the wheel matching this machine's
# platform tags. See vendor/README.md.
--find-links=../vendor
pymumble==1.7
python-dotenv>=1.0.0
# openai-whisper pulls in torch (large download) and numpy
openai-whisper>=20231117
openai>=1.0.0
numpy>=1.24.0